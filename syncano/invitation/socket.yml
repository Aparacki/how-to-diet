name: invitation
description: Socket for Digitalsign invitations

endpoints:
  list:
    description: Endpoint to get a list of user's invitations
    inputs:
      properties:
        documentId:
          type: integer
    outputs:
      success:
        exit_code: 200
        description: Success
      fail:
        exit_code: 400
        description: Failure
